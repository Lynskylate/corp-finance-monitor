# systemd units

These `.in` files are templates for `deploy.sh` (uses `sed` for variable substitution).
For ansible deployment, use the Jinja2 templates in `deploy/ansible/roles/cfm/templates/` instead.
